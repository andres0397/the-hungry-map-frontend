export interface AuthTokenEntity {
  accessToken: string;
}

export interface UserEntity {
  createdAt: string;
  email: string;
  id: number;
  name: string;
  password: string;
  status: string;
  updatedAt: string;
}
